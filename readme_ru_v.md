# Beam Profile Analyzer — Поток данных и алгоритмы обработки

---

## 1. Общее описание

Приложение захватывает кадры с FireWire-камеры (IEEE 1394, протокол IIDC, pydc1394),
вычитает предварительно собранный фон и рассчитывает характеристики пучка (центроид,
СКО размера пучка) по результирующему разностному изображению. Результаты можно
экспортировать в виде CSV/JSON-файлов или PNG-изображений.

---

## 2. Полная схема потока данных

```
╔══════════════════════════════════════════════════════════════════════════════════════════════╗
║                                 КАМЕРА (FireWire / IIDC)                                     ║
║                     Формат пикселя: MONO16 / Y16, big-endian, uint16                         ║
╚══════════════════════════════════════════════════════════════════════════════════════════════╝
                                           │
                      сырые байты (2 байта на пиксель, big-endian)
                                           │
                                           ▼
╔══════════════════════════════════════════════════════════════════════════════════════════════╗
║                         CameraManager._decode_frame()                                        ║
║                                                                                              ║
║  arr = np.frombuffer(raw_bytes, dtype='>u2').reshape((H, W))                                 ║
║  Возвращает arr.copy()                                                                       ║
╚══════════════════════════════════════════════════════════════════════════════════════════════╝
                                           │
                         ndarray формы (H, W), dtype=uint16
                                           │
               ┌───────────────────────────┴──────────────────────────┐
               │ Путь КАДРА                                           │ Путь ФОНА
               ▼                                                      ▼
╔══════════════════════════╗                           ╔══════════════════════════════════════╗
║  raw_current_frame       ║                           ║  capture_background_frames()         ║
║  (хранится «как есть»)   ║                           ║                                      ║
║  dtype: uint16           ║                           ║  N=40 кадров, каждый приводится к    ║
╚══════════════════════════╝                           ║  float64, массив (N, H, W)           ║
               │                                       ╚══════════════════════════════════════╝
               │                                                       │
               │                                   ndarray (N, H, W), dtype=float64
               │                                                       │
               │                                                       ▼
               │                              ╔══════════════════════════════════════════════╗
               │                              ║  ImageNormalizer.process_background()        ║
               │                              ║                                              ║
               │                              ║  raw_bg = np.mean(frames, axis=0)            ║
               │                              ║  raw_bg[raw_bg < 0] = 0                      ║
               │                              ╚══════════════════════════════════════════════╝
               │                                                       │
               │                              raw_bg: ndarray (H, W), dtype=float64
               │                              (сохраняется как raw_background)
               │                                                       │
               ▼                                                       ▼
╔══════════════════════════════════════════════════════════════════════════════════════════════╗
║                    ImageNormalizer.normalize()  ×2                                           ║
║                                                                                              ║
║  current_frame = normalize(raw_current_frame)                                                ║
║    → image_f64 = frame.astype(float64)                                                       ║
║    → min_val, max_val — из самого кадра                                                      ║
║    → result = (image_f64 − min_val) / (max_val − min_val)    ∈ [0, 1]                        ║ 
║                                                                                              ║
║  background = normalize(raw_bg, reference_image=raw_current_frame)                           ║
║    → min_val, max_val берутся из raw_current_frame                                           ║
║    → result = (raw_bg_f64 − min_val) / (max_val − min_val)                                   ║
║    (та же шкала, что и у current_frame; значения могут незначительно выходить за [0, 1])     ║
╚══════════════════════════════════════════════════════════════════════════════════════════════╝
               │                                                       │
  current_frame: float64 ∈ [0,1]                  background: float64 (шкала референса)
               │                                                       │
               └───────────────────┬───────────────────────────────────┘
                                   │
                                   ▼
╔══════════════════════════════════════════════════════════════════════════════════════════════╗
║                      ImageReader._calculate_difference()                                     ║
║                                                                                              ║
║  raw_diff = raw_current_frame.astype(float64) − raw_background.astype(float64)               ║
║  raw_diff[raw_diff < 0] = 0                     ← обрезка отрицательных значений             ║
║  difference = ImageNormalizer.normalize(raw_diff)                                            ║
║    → min_val = min(raw_diff),  max_val = max(raw_diff)                                       ║
║    → difference = (raw_diff − min_val) / (max_val − min_val)    ∈ [0, 1]                     ║
╚══════════════════════════════════════════════════════════════════════════════════════════════╝
                                           │
                  difference: ndarray (H, W), dtype=float64 ∈ [0, 1]
                                           │
              ┌────────────────────────────┼────────────────────────────┐
              │                            │                            │
              ▼ ТОЛЬКО ДИСПЛЕЙ            ▼ АНАЛИЗ                    ▼ ЭКСПОРТ
```

---

## 3. ROI — область интереса

### 3.1 Как задаётся ROI

Пользователь рисует прямоугольник на тепловой карте перетаскиванием левой кнопкой мыши.
Координаты захватываются в **миллиметрах** (пространство данных осей графика). Затем
они переводятся в **пиксельные индексы**:

```
x_min = round( x0_mm / pixel_size_x  +  img_width  / 2 )
x_max = round( x1_mm / pixel_size_x  +  img_width  / 2 )
y_min = round( y0_mm / pixel_size_y  +  img_height / 2 )
y_max = round( y1_mm / pixel_size_y  +  img_height / 2 )
```

Начало координат — **центр изображения**. Положительные X — вправо, Y — вверх.
Результаты ограничиваются диапазоном `[0, img_width]` и `[0, img_height]`.

Пиксельная ROI `(x_min, y_min, x_max, y_max)` сохраняется в `ImageAnalyzer.roi`.

### 3.2 Как ROI применяется к изображению

```python
cropped = image[y_min:y_max, x_min:x_max]   # срез numpy-массива
```

Этот обрезанный подмассив используется как вход для **всех** последующих вычислений
(проекции, центроид, СКО).

### 3.3 Координатная сетка внутри ROI

После обрезки индексы пикселей внутри подмассива начинаются с 0:

```
i = 0, 1, …, (x_max − x_min − 1)   — локальные индексы столбцов внутри ROI
j = 0, 1, …, (y_max − y_min − 1)   — локальные индексы строк внутри ROI
```

Эти **локальные** индексы используются как есть для вычисления проекций и СКО.

Для **центроида** локальный индекс пересчитывается в глобальные координаты полного
кадра путём добавления смещения ROI:

```
c_x_global = c_x_local + x_min_roi
c_y_global = c_y_local + y_min_roi
```

Это гарантирует, что центроид всегда выражается относительно **центра полного
(необрезанного) кадра**, независимо от расположения ROI.

### 3.4 ROI при экспорте

При нажатии «Export Data» с активной ROI все массивы изображений (shot, background,
difference) физически обрезаются до границ ROI **перед** сохранением. Поле `resolution`
в метаданных обновляется соответственно.

```
┌─────────────────────────────────────────────────────────────────┐
│               Полное изображение  (H × W)                       │
│                                                                 │
│   ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐                             │
│   │                               │                             │
│   │        ROI-подмассив          │  ← используется для         │
│   │   image[y_min:y_max,          │    всего анализа            │
│   │          x_min:x_max]         │                             │
│   │                               │                             │
│   └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. Алгоритмы анализа

Все алгоритмы работают с **разностным** изображением (float64, ∈ [0,1]).
Если ROI активна, изображение сначала обрезается (см. §3.2).

---

### 4.1 Проекции

```
calculate_projections(image, pixel_size_x, pixel_size_y)
```

**Шаг 1 — Суммирование вдоль каждой оси (маргинальные распределения):**

```
P_x[i] = Σ_j  image[j, i]       (сумма по всем строкам для столбца i)
P_y[j] = Σ_i  image[j, i]       (сумма по всем столбцам для строки j)
```

**Шаг 2 — Нормировка на максимум:**

```
P_x[i] = P_x[i] / max(P_x)
P_y[j] = P_y[j] / max(P_y)
```

**Шаг 3 — Перевод индекса пикселя в миллиметры:**

```
x_coords[i] = i · pixel_size_x       (мм, начало — левый край ROI)
y_coords[j] = j · pixel_size_y       (мм, начало — верхний край ROI)
```

**Выход:** `(x_coords [мм], P_x [отн. ед.], y_coords [мм], P_y [отн. ед.])`

---

### 4.2 Центроид

```
calculate_centroid(image, pixel_size_x, pixel_size_y)
```

Функция принимает **полный (необрезанный) кадр**. Размеры полного кадра
сохраняются до обрезки, ROI применяется внутри функции.

**Шаг 1 — Сохранение размеров полного кадра (до обрезки):**

```
full_H, full_W = image.shape
```

**Шаг 2 — Обрезка по ROI:**

```
processed_image = image[y_min:y_max, x_min:x_max]   (подмассив H' × W')
```

**Шаг 3 — Маргинальные проекции (без нормировки) на обрезанном изображении:**

```
P_x[i] = Σ_j  processed_image[j, i]
P_y[j] = Σ_i  processed_image[j, i]
```

**Шаг 4 — Взвешенное среднее положение в локальных пикселях ROI:**

```
            Σ_i  i · P_x[i]                    Σ_j  j · P_y[j]
c_x_loc = ─────────────────────    c_y_loc = ─────────────────────
                Σ_i  P_x[i]                         Σ_j  P_y[j]
```

**Шаг 5 — Перевод из локальных пикселей ROI в глобальные пиксели полного кадра:**

```
c_x_px = c_x_loc + x_min_roi
c_y_px = c_y_loc + y_min_roi
```

Этот шаг **пропускается**, если ROI не активна (`c_x_px = c_x_loc`).

**Шаг 6 — Перевод в мм с центрированием относительно центра полного кадра:**

```
centroid_x = ( c_x_px  −  full_W / 2 ) · pixel_size_x   [мм]
centroid_y = ( c_y_px  −  full_H / 2 ) · pixel_size_y   [мм]
```

> **Ключевое свойство:** Центроид всегда отсчитывается относительно
> **геометрического центра полного кадра сенсора** — как при активной ROI,
> так и без неё. Выбор ROI не смещает начало координат — он лишь ограничивает
> пиксели, участвующие во взвешенном усреднении.

**Выход:** `(centroid_x [мм], centroid_y [мм])`

---

### 4.3 СКО (RMS) размера пучка

```
calculate_rms(image, pixel_size_x, pixel_size_y)
```

**Шаг 1 — Маргинальные проекции и центроид (те же, что в §4.2).**

**Шаг 2 — Взвешенное стандартное отклонение (в пикселях):**

```
            ┌  Σ_i  P_x[i] · (i − c_x_px)²  ┐½
rms_x_px =  │ ──────────────────────────────  │
            └         Σ_i  P_x[i]             ┘

            ┌  Σ_j  P_y[j] · (j − c_y_px)²  ┐½
rms_y_px =  │ ──────────────────────────────  │
            └         Σ_j  P_y[j]             ┘
```

**Шаг 3 — Перевод в миллиметры:**

```
rms_x = rms_x_px · pixel_size_x   [мм]
rms_y = rms_y_px · pixel_size_y   [мм]
```

**Выход:** `(rms_x [мм], rms_y [мм])`

---

## 5. Размер пикселя

Размер пикселя (мм/пикс) определяется автоматически по разрешению сенсора:

| Разрешение сенсора | Размер пикселя (мм) |
|---|---|
| 1032 × 776  | 0.02840909 |
| 1624 × 1224 | 0.01875468 |
| прочие      | 0.01 (по умолчанию) |

Одни и те же значения `pixel_size_x` и `pixel_size_y` используются для всех
координатных преобразований в §4 и для осей на графиках проекций.

---

## 6. Что идёт на дисплей, что — в файлы

```
              difference: float64 (H, W) ∈ [0, 1]
                         │
        ┌────────────────┴──────────────────────────┐
        │ ТОЛЬКО ДИСПЛЕЙ                            │ ДИСПЛЕЙ + ЭКСПОРТ
        │                                           │
        ▼                                           ▼
╔═══════════════════════╗          ╔═══════════════════════════════════════════════╗
║  PlotManager          ║          ║  ImageAnalyzer                                ║
║                       ║          ║                                               ║
║  imshow(difference,   ║          ║  Если ROI: image → image[y0:y1, x0:x1]        ║
║    cmap='jet',        ║          ║                                               ║
║    vmin=0, vmax=1,    ║          ║  calculate_projections() → x, P_x, y, P_y     ║
║    origin='lower')    ║          ║  calculate_centroid()    → cx, cy [мм]        ║
║                       ║          ║  calculate_rms()         → rx, ry [мм]        ║
║  Графики проекций     ║          ╚═══════════════════════════════════════════════╝
║  только на экране     ║                          │
╚═══════════════════════╝               Статистика пучка — в метках UI
        │                               и в метаданных экспорта
   только экран
        │
        └─── НЕ записывается в CSV и PNG-файлы


                  ┌───────────────────────────┐
                  │         ЭКСПОРТ           │
                  └───────────────────────────┘

  При нажатии «Export Data» данные (при активной ROI — обрезанные) сохраняются:

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  Экспорт CSV / JSON  (DataExporter.export_csv)                               │
  │                                                                              │
  │  metadata.json ── скалярные значения:                                        │
  │    resolution, pixel_size_x, pixel_size_y, date,                             │
  │    centroid_x_mm, centroid_y_mm, rms_x_mm, rms_y_mm,                         │ 
  │    roi_active, roi_pixels, roi_mm                                            │
  │                                                                              │
  │  shot.csv        ── float64, 17 знаков после запятой, форма (H, W)           │
  │  background.csv  ── float64, 17 знаков после запятой, форма (H, W)           │
  │  difference.csv  ── float64, 17 знаков после запятой, форма (H, W)           │
  │  raw_shot.csv    ── исходные данные камеры uint16, форма (H, W)              │
  └──────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  Экспорт PNG  (DataExporter.export_png)                                      │
  │                                                                              │
  │  Для каждого из shot / background / difference:                              │
  │    fig size = (W px / dpi, H px / dpi) при dpi=100 → точное совпадение       │
  │    imshow(plain, cmap='jet', vmin=0, vmax=1, origin='lower')                 │
  │    сохраняется <name>.png — та же цветовая шкала, что на экране              │
  │                                                                              │
  │  metadata.json ── resolution, pixel_size, date, rms_x_mm, rms_y_mm           │
  └──────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. Таблица типов данных по этапам

| Этап | Переменная | dtype | Диапазон значений |
|---|---|---|---|
| Сырой буфер камеры | `raw_bytes` | bytes | — |
| После `_decode_frame` | `raw_current_frame` | uint16 | 0 … 65535 |
| Массив кадров фона | `background_frames` | float64 | 0 … 65535 |
| Усреднённый сырой фон | `raw_background` | float64 | 0 … 65535 |
| Нормированный текущий кадр | `current_frame` | float64 | [0, 1] |
| Нормированный фон | `background` | float64 | ≈ [0, 1] |
| Разностное изображение (норм.) | `difference` | float64 | [0, 1] |
| Центроид | `centroid_x/y` | float64 | мм, знаковый |
| СКО | `rms_x/y` | float64 | мм, ≥ 0 |
| Координаты проекций | `x_coords`, `y_coords` | float64 | мм |
| Значения проекций | `P_x`, `P_y` | float64 | [0, 1] |
| Ячейки CSV | — | текст (float64) | 17 знаков |

---

## 8. Полная блок-схема

```
┌────────────────────────────────────────────────────────────────────────────────────┐
│                          КАМЕРА  (FireWire IIDC)                                   │
│  Сенсор: MONO16, big-endian uint16  │  pixel_size: по таблице разрешений (мм)      │
└────────────────────────────────────────────────┬───────────────────────────────────┘
                                                 │ сырые байты (>u2)
                                                 ▼
┌────────────────────────────────────────────────────────────────────────────────────┐
│  _decode_frame()                                                                   │
│  np.frombuffer(raw, dtype='>u2').reshape(H, W) → ndarray uint16                    │
└─────────────────┬──────────────────────────────────────────────────┬───────────────┘
                  │ uint16 (H×W)                                     │ uint16 (H×W) × N кадров
                  ▼                                                  ▼
┌─────────────────────────────────────┐      ┌────────────────────────────────────────┐
│  raw_current_frame  (uint16)        │      │  capture_background_frames()           │
│  хранится для вычисления разности   │      │  frames.astype(float64)                │
└─────────────────────────────────────┘      │  → ndarray (N, H, W) float64           │
                  │                          └──────────────────┬─────────────────────┘
                  │                                             │ (N,H,W) float64
                  │                                             ▼
                  │                          ┌────────────────────────────────────────┐
                  │                          │  process_background()                  │
                  │                          │  raw_bg = mean(frames, axis=0)         │
                  │                          │  raw_bg[raw_bg < 0] = 0                │
                  │                          │  → raw_background  float64 (H×W)       │
                  │                          └──────────────────┬─────────────────────┘
                  │                                             │ float64 (H×W)
                  ▼                                             ▼
┌────────────────────────────────────────────────────────────────────────────────────┐
│  _calculate_difference()                                                           │
│  raw_diff = raw_current.astype(f64) − raw_background.astype(f64)                   │
│  raw_diff[raw_diff < 0] = 0                                                        │
│  difference = normalize(raw_diff)  →  float64 ∈ [0, 1]                             │
└─────────────────────────────────┬──────────────────────────────────────────────────┘
                                  │ difference: float64 (H×W) ∈ [0,1]
                     ┌────────────┴─────────────┐
                     │                          │
                     ▼ ДИСПЛЕЙ                  ▼ АНАЛИЗ И ЭКСПОРТ
          ┌────────────────────────┐  ┌──────────────────────────────────────────────┐
          │  PlotManager           │  │  calculate_centroid() получает ПОЛНЫЙ кадр   │
          │  imshow jet,           │  │  full_W, full_H = image.shape                │
          │  vmin=0 vmax=1         │  │  crop → image[y0:y1, x0:x1]  (H'×W')        │
          │  Графики проекций      │  └───────────────────┬──────────────────────────┘
          │  только на экране      │                      │ полный кадр float64 (H×W)
          └────────────────────────┘         ┌────────────┴────────────────┐
                                             │                             │
                                             ▼                             ▼
                                  ┌──────────────────────────┐  ┌────────────────────────────┐
                                  │  calculate_centroid()    │  │  calculate_rms()           │
                                  │                          │  │  crop→ROI(H'×W') внутри    │
                                  │  full_W, full_H сохр.    │  │  P_x = sum(roi, ax=0)      │
                                  │  crop → ROI (H'×W')      │  │  P_y = sum(roi, ax=1)      │
                                  │  P_x = sum(roi, ax=0)    │  │                            │
                                  │  P_y = sum(roi, ax=1)    │  │  c_x_loc=Σ(i·P_x)/ΣP_x     │
                                  │                          │  │                            │
                                  │  c_x_loc=Σ(i·P_x)/ΣP_x   │  │  σ_x_px =                  │
                                  │  c_x_px=c_x_loc+x_min    │  │  √(Σ P_x·(i−c_x)²/ΣP_x)    │
                                  │  c_y_px=c_y_loc+y_min    │  │                            │
                                  │                          │  │  rms_x = σ_x_px · Δx       │
                                  │  cx=(c_x_px−full_W/2)·Δx │  │  rms_y = σ_y_px · Δy       │
                                  │  cy=(c_y_px−full_H/2)·Δy │  │  → rms_x, rms_y  [мм]      │
                                  │  → cx, cy  [мм]          │  │                            │
                                  │  (относит. центра        │  │  (СКО — в локальных        │
                                  │   полного кадра)         │  │   пикселях ROI)            │
                                  └──────────────────────────┘  └────────────────────────────┘
                                             │                             │
                                             └──────────────┬──────────────┘
                                                            │
                                  ┌─────────────────────────┴───────────────────────────┐
                                  │  ЭКСПОРТ                                            │
                                  │                                                     │
                                  │  CSV/JSON:                                          │
                                  │    metadata.json  ← скаляры (cx, cy, rms, …)        │
                                  │    shot.csv       ← float64, 17 знаков, (H×W)       │
                                  │    background.csv ← float64, 17 знаков, (H×W)       │
                                  │    difference.csv ← float64, 17 знаков, (H×W)       │
                                  │                                                     │
                                  │  PNG:                                               │
                                  │    shot.png       ← цветовая карта jet, [0,1]       │
                                  │    background.png ← та же шкала                     │
                                  │    difference.png ← та же шкала                     │
                                  │    metadata.json  ← rms_x_mm, rms_y_mm, …           │
                                  └─────────────────────────────────────────────────────┘
```

---

## 9. Примечания

* **Порядок байт:** Все используемые камеры — FireWire IIDC (Flea2 и аналоги).
  Они всегда передают данные в порядке **big-endian**. Декодер использует
  `dtype='>u2'` (big-endian, беззнаковое 16-битное) безусловно.

* **Нормировка фона:** Фоновое изображение нормируется с использованием
  *текущего кадра* в качестве референса. Это означает, что `background` и
  `current_frame` находятся на одной шкале интенсивности — их разность
  физически корректна.

* **Отрицательные значения:** Вычитание сырого фона из сырого кадра может
  давать отрицательные значения (шум, флуктуации тёмного тока). Они обрезаются
  до нуля перед нормировкой.

* **Центроид и СКО в мм:** Все пространственные характеристики вычисляются
  **сначала в пространстве пикселей**, затем умножаются на размер пикселя
  из таблицы. Промежуточная сетка в миллиметрах для интегрирования не строится.

* **Влияние ROI на центроид/СКО:** При активной ROI центроид по-прежнему
  выражается относительно **центра полного кадра сенсора**. Алгоритм вычисляет
  взвешенный центр в локальных пикселях ROI, затем прибавляет смещение угла ROI
  `(x_min_roi, y_min_roi)` для получения глобального пиксельного индекса, и лишь
  после этого переводит в мм. Это означает, что значение центроида непосредственно
  сравнимо между измерениями с ROI и без неё.
  СКО, напротив, является мерой разброса (второй центральный момент) и вычисляется
  целиком в локальных пикселях ROI — оно отражает лишь размер пучка в пределах
  выбранной области.

* **Медианный фильтр:** Код содержит функцию `apply_median_filter`, однако
  она **отключена** (закомментирована). Никакая пространственная фильтрация
  к данным не применяется.

* **Аппроксимация Гаусса:** Функция `fit_gaussian` присутствует в классе
  `ImageAnalyzer`, но **не вызывается** в текущем конвейере обработки и не
  влияет на отображаемые или экспортируемые значения.